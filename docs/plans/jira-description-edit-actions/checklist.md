# 설명 편집 버튼 — 구현 체크리스트

## 1. 구현

- [x] 읽기/편집 컨테이너 `data-testid` 확인
- [x] `설명` 라벨이 편집 중에도 남는지 확인
- [x] 아래쪽 취소 버튼 앵커 확인 (`comment-cancel-button`)
- [x] 라벨 줄 헬퍼를 `descriptionMarkdownCopy` 와 공유 (`descriptionLabel.ts`)
- [x] 카탈로그·기본 설정 등록 (ON)
- [x] 런타임 구현. **설명 편집 컨테이너 안에서만** 버튼을 찾는다
- [x] `저장` 버튼 추가 (`취소` 왼쪽, 다른 모양)
- [x] content entry 에 연결

## 2. 테스트

- [x] 아래쪽 취소·저장을 각각 대신 누르는지
- [x] 저장이 취소와 다르게 그려지는지
- [x] 문서 전체에서 찾지 않는지 (댓글 취소 방지)
- [x] 편집 중이 아니면 붙지 않는지
- [x] `npm run check`

## 3. 실사용 확인

- [ ] 편집을 시작하면 `설명` 옆에 `저장`·`취소` 가 나타나는지
- [ ] 각각 아래쪽 `저장`·`취소` 와 똑같이 동작하는지
- [ ] 편집을 끝내면 버튼이 사라지는지
- [ ] **댓글을 쓰는 중에 설명 취소 버튼이 나타나지 않는지**
- [ ] 내용을 고친 뒤 눌렀을 때 Jira 기본 확인 흐름이 그대로인지

## 4. 문서

- [x] `spec/features/` 기능 문서
- [x] `spec/product-overview.md` 변경 이력
- [x] `README.md`
