# Inno Extension

사내 업무 사이트 5곳(아마란스 · Jira · Confluence · GitHub Enterprise · GitLab)의 반복 작업을
하나의 Chrome 확장으로 묶은 도구다. 각 사이트 화면에 버튼을 주입해 링크·번호·본문을 원하는
형식으로 복사하거나, 화면 구조를 사용자가 원하는 대로 바꾼다.

**API를 쓸 수 있다. 다만 최대한 쓰지 않는다.**

기본은 이미 로드된 화면의 DOM만 읽고 쓰는 것이다. DOM으로 안 되는 것만 API를 쓰고, 그런 기능은
**특별 관리 대상**으로 따로 다룬다.

### 특별 관리란 (2026-09-18 확정)

| 규칙 | 내용 |
| --- | --- |
| 표시 | 카탈로그의 `usesNetwork: true`. Popup에도 배지로 보인다 |
| 범위 | **읽기(GET)만.** 쓰기가 필요하면 그때 따로 판단한다 |
| 시점 | **사용자가 그 기능을 실제로 쓸 때만.** 화면을 여는 것만으로 요청하지 않는다 |
| 오리진 | 그 사이트의 오리진으로만. 세션 쿠키를 쓰고 토큰을 저장하지 않는다 |
| 문서 | `spec/features/`에 **어떤 요청을 언제 보내는지** 적는다 |

**이 규칙은 테스트로 막혀 있다.** `usesNetwork`로 표시되지 않은 기능의 파일에서 네트워크
호출이 발견되면 테스트가 깨진다. 문장만 있으면 지켜지지 않는다.

Chrome 웹 스토어에 올리지 않는다. 사용자는 릴리즈 ZIP을 내려받아 **압축해제된 확장**으로
설치한다.

## 기술 스택

| 항목 | 값 |
| --- | --- |
| 언어 | TypeScript (strict) |
| 번들러 | Vite + `@crxjs/vite-plugin` |
| 확장 | Chrome MV3 (content script 중심, background 최소) |
| 테스트 | `node --test` (Vite SSR 번들 후 실행) |
| 외부 런타임 의존성 | `marked` 하나 |

## 구조

| 경로 | 책임 |
| --- | --- |
| `src/catalog/` | 사이트·기능 목록의 **정본**. 새 기능은 여기부터 등록한다 |
| `src/platform/` | 사이트와 무관한 공용 기반 — 런타임 루프, 설정 저장소, 클립보드, 메시징 |
| `src/sites/<siteId>/` | 사이트별 구현. `features/<featureId>/`로 다시 나뉜다 |
| `src/popup/` | 설정 화면 |
| `src/background/` | MV3 service worker |
| `spec/` | 제품·기능 명세. 행동 계약과 변경 이력 |
| `docs/` | 작업 계획, 조사 기록, 용어, 오류 기록 |
| `tests/` | 단위 테스트 |
| `scripts/` | 빌드·패키징 보조 |
| `.codex/skills/` | 이 저장소 전용 작업 스킬 (릴리즈, 설치본 갱신) |

### 사이트 런타임의 동작 방식

각 사이트는 `createSiteRuntime`으로 시작한다. **MutationObserver 하나**가 DOM 변화를 감지해
디바운스 후 등록된 기능들의 `reconcile(context)`를 부른다. 기능은 "지금 화면에 무엇이 있어야
하는가"만 판단하고, 있으면 두고 없으면 만든다. **멱등해야 한다.**

주입한 UI는 Shadow DOM으로 격리한다. 사이트 스타일과 섞이면 사이트 변경에 깨진다.

## 프로젝트 규칙 (my-app-init, 2026-09-15 확정)

### git

- author: `정재원/AI BE개발 챕터/II <JeongJaew0n@users.noreply.github.com>` — `git config --local`로
  설정돼 있다. 커밋 전 `git config user.email`로 확인한다.
  **이 저장소는 공개다.** 개인·회사 이메일을 커밋에 남기지 않기 위해 GitHub noreply를 쓴다.
- 커밋·푸시: **커밋만 자동.** 작업 단위가 끝나면 알아서 커밋하고, **푸시는 사용자 확인을 받는다.**
- 브랜치: **현행 유지.** `develop`에서 작업하고, 릴리즈 때 `main`을 fast-forward로 올린다.
  `main`에 직접 커밋하지 않는다.
- 커밋은 원자적 단위로 나눈다. 한 커밋이 두 가지 일을 하면 파일을 나눠서라도 쪼갠다.
- **예외 — 리뷰는 자동 커밋·푸시하지 않는다.** 코드리뷰·리뷰 문서, 그리고 리뷰에서
  나온 수정은 위 정책이 '커밋만 자동'이어도 사람이 읽고 판단한 뒤에 커밋한다.
  리뷰는 사실이 아니라 의견이고, 틀린 의견이 먼저 기록에 박히면 되돌리기 어렵다.

### docs

- 여러 단계짜리 작업은 코드를 건드리기 전에 `docs/plans/<slug>/`에 계획을 먼저 쓴다.
  이 저장소는 `spec.md` · `context.md` · `checklist.md` 세 파일 구조를 쓴다.
- 원인 찾는 데 시간이 걸린 오류는 `docs/troubleshootings/`에 남긴다.
  원인이 라이브러리·런타임·OS에 있으면 `reusable/`, 이 프로젝트의 코드·설정에 있으면
  `project-specific/`.
- 도메인 용어를 새로 만들거나 이름을 바꾸면 `docs/glossary/README.md`를 먼저 고치고
  코드를 그 이름에 맞춘다. 코드만 바꾸면 용어집이 거짓말이 된다.

> **기존 폴더와의 관계.** `docs/issue/`는 *아직 원인이 확정되지 않았거나 조치가 남은* 문제를,
> `docs/postmortems/`는 *이미 해결된 사고*를 담는다. 상태로 나눈 축이다. 새로 생긴
> `docs/troubleshootings/`는 *원인이 어디에 있는가*로 나눈 축이라 기준이 다르다.
> **정리 방침은 아직 정하지 않았다.** 지금은 셋을 그대로 두고, 새 기록은 위 규칙을 따른다.

### 버전

- **0.x.x에 머문다. 1.0.0으로 올리지 않는다.** 사내에서 쓰는 도구라 공개 API 안정성을
  약속하지 않는다.
- 규칙은 Semantic Versioning을 따르되 0.x 구간의 관례를 쓴다.

  | 변경 | 올리는 자리 | 예 |
  | --- | --- | --- |
  | 호환이 깨지는 변경 | **MINOR** (0.y) | `0.8.0` → `0.9.0` |
  | 기능 추가 (호환 유지) | MINOR | `0.8.0` → `0.9.0` |
  | 버그 수정·내부 정리 | PATCH | `0.8.0` → `0.8.1` |

  0.x에는 올릴 MAJOR가 없어서 MINOR가 그 역할을 대신한다.
- 번호가 사는 곳은 **`package.json`의 `version`이 정본이다.**
  `package-lock.json`과 `manifest.json`은 릴리즈 절차가 같은 값으로 맞춘다.
  **릴리즈 외의 경로로 세 파일을 손으로 고치지 않는다.**
- 릴리즈는 `.codex/skills/extension-release/`가 담당한다. 버전 결정·패키징·태그·GitHub
  Release 발행까지 그 절차를 따른다.
- 버전을 올린 커밋에는 무엇이 올랐는지 한 줄 적는다.

### 설계

- 기능 묶음 단위는 **사이트(site)** 다. 새 기능은 기존 사이트에 넣을지 새 사이트를 추가할지
  먼저 정하고 시작한다. 코드의 디렉터리·타입 이름도 이 말을 쓴다(`src/sites/<siteId>/`,
  `SiteId`, `SiteDescriptor`).
- 새 기능을 넣는 순서는 **카탈로그 → 설정 기본값 → 사이트 런타임 → spec → README**다.
  카탈로그(`src/catalog/`)에 등록하지 않은 기능은 Popup에 나타나지 않는다.
- 사이트 화면을 겨냥하는 선택자는 **의미 있는 속성**을 쓴다. 빌드마다 바뀌는 해시 클래스에
  의존하지 않는다.

## 확인하지 않은 것을 단정하지 않는다

이 저장소의 기능은 전부 **남의 사이트 DOM**에 기대고 있다. 그래서 "이렇게 동작할 것"이라는
추측이 자주 틀린다. 실제로 이 저장소의 `docs/`에는 추측으로 내린 결론을 나중에 실측으로
뒤집은 기록이 여러 건 있다.

- 동작을 주장하기 전에 **브라우저에서 실제로 측정한다.**
- 측정하지 못했으면 측정하지 못했다고 쓴다.
- 나쁜 결과도 그대로 보고한다.
